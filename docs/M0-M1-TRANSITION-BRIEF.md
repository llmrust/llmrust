# M0 → M1 过渡简报

**日期**：2026-07-24 ｜ **架构师**：Kimi ｜ **面向**：llmrust Owner
**基线**：`main` @ `ceccd9b`（STATE-REL-001 合并后）

---

## 一、M0 五项成果回顾

| 任务 | 交付 | 可观察结果 |
|---|---|---|
| SPEC-000 | 治理章程入库 | 任务/Issue/PR/状态回证流程有法可依 |
| CI-001 | CI 基线门禁 | fmt/clippy/MSRV/Test 全绿成为合并前提，旧 run 自动取消 |
| CI-002 | 安全与供应链门禁 | RustSec 漏洞、违规许可证、git 野生依赖、密钥泄漏 → 自动拒收 |
| CI-003 | 架构与发布包守卫 | 依赖边、7 个超大文件台账（只减不增）、package allowlist → 机器执行；实战中已抓住一次真实违规 |
| REL-001 | Tag-only 发布流水线 | 0.1.2 事故四根因（dirty/无 tag/版本漂移/手工旁路）机制性封死；dry-run 零上传演练通过 |

外加两项治理任务（SPEC-001 角色更换、SPEC-002 规范体系统一）与三份勘误（E-001 已修复；E-002/E-003 已映射到 M2 任务）。

## 二、当前基线状态

- **门禁**：每个 PR 六项检查（MSRV 1.86 / Test stable / Architecture guards / RustSec / cargo-deny / gitleaks）+ tag 触发的 release 流水线
- **台账**：7 个 >800 行热点文件只减不增（proxy/mod.rs 基线 2582，含一次架构师特批调整记录）
- **发布**：`cargo publish` 在机制上只能经 tag + 干净树 + 四闸校验 + 环境审批的 dry-run 路径；**§3.3 发布冻结仍有效**
- **版本**：0.1.1（Cargo.toml / capabilities / CHANGELOG 一致）

## 三、M1 四任务：优先级与依赖

| 任务 | 内容 | 依赖 | 可并行性 | 建议顺序 |
|---|---|---|---|---|
| **API-001** | 完整 public API 清单 + 逐项分类 | M0 DONE（已满足） | 阻塞下游全部 | **第 1**（它是 M1 的测量基线） |
| **API-002** | Semver 检查机器化（对比 0.1.1/0.1.2） | API-001 清单 | 依赖 | 第 2 |
| **API-003** | Retry 契约冻结（含 E-002 警告放大修复） | API-001 | 依赖 | 第 3 |
| **DOC-001** | `COMPATIBILITY-0.1.3.md` 兼容性说明 | API-001/002/003 结论 | 依赖 | 第 4（收口） |

**结论：M1 四个任务是严格串行链，不可并行**——API-001 是唯一 READY，先做。

## 四、0.1.3 发布决策清单

### A. 解除冻结的前置拍板项（Owner 决策）

- [ ] 是否发 0.1.3？（M1–M3 未做时发布 = 接受 API 未冻结的风险）
- [ ] 发布的最低门槛：等 M1 完？M2 完？还是全量？
- [ ] CHANGELOG 口径：0.1.3 相对受污染的 0.1.2 如何措辞
- [ ] 解除冻结的书面批准形式（Issue 决议 / SPCC 状态行变更）

### B. 发布本身的执行项（流水线机械动作）

- [ ] 打 `v0.1.3` tag（版本四处一致由 gate2 强制）
- [ ] release 环境审批放行 dry-run
- [ ] 核验 hash/provenance 样例
- [ ] **最后一次真实 publish 仍需 Owner 单独显式批准**（流水线只干到 dry-run）

## 五、架构师建议

先启动 API-001（public API 清单 + 分类），它是 M1 的测量基线也是唯一 READY 任务；0.1.3 的发布决策**建议至少等到 M1 DONE**——否则发布的仍是一个 API 未冻结、与 0.1.2 关系未书面化的版本。
